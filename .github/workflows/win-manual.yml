name: WindowsをランナーにしたManual
on:
  workflow_dispatch:
    inputs:
      greeting:
        type: string
        default: Hello
        required: true
        description: A cheerful word
jobs:
  run:
    runs-on: windows-latest
    steps:
      - run: echo "${{inputs.greeting}}"
